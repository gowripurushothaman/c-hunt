#include<conio.h>
#include<conio.h>
#include<string.h>
int main()
{
char nam[10];
int dec=0,i,len;
gets(num);
len=strlen(num);
for(i=0;i<num;i++)
{
dec=dec*10+(a[i]-'0');
}
printf("%d",dec);
return 0;
}
